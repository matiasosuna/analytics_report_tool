name: analytics_report_tool
description: A new Flutter package project.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  dartz: ^0.10.1
  firebase_analytics: ^9.1.10
  flutter:
    sdk: flutter
  freezed: ^2.0.3+1

dev_dependencies:
  build_runner: ^2.1.11
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  freezed_annotation: ^2.0.3


flutter:
